# OpenRouter API key — same key Claude Code uses as ANTHROPIC_AUTH_TOKEN
OPENROUTER_API_KEY=

# How often (seconds) to poll OpenRouter for credit totals
POLL_INTERVAL_SEC=30

# Backend port (Vite dev server proxies /api here)
PORT=3001
